version: "3"

services:
  study-db:
    image: postgres
    ports:
      - 5432
    environment:
      POSTGRES_DB: testcontainers
      POSTGRES_PASSWORD: testcontainers
      POSTGRES_USER: testcontainers
